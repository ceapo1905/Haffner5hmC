library(foreign)
read.dta("../penile_tma_dataset_full.dta")
data <- read.dta("../penile_tma_dataset_full.dta")
View(data)
data <- read.csv("../tma852-855_dataset.csv")
View(data)
DataFull <- read.csv("../tma852-855_dataset.csv")
DataFull <- read.csv("../tma852-855_dataset.csv")
Variables <- c("caseid", "subtype", "grade", "X5hmc")
Data <- DataFull[, Variables]
View(Data)
DataCases <- DataFull[, Variables]
DataSelected <- DataFull[, Variables]
?subset
Data <- subset(DataSelected, !is.na(X5hmc))
DataSelected <- DataFull[, Variables]
Data <- subset(DataSelected, X5hmc == !is.na)
View(DataFull)
?read.csv
DataFull <- read.csv("../tma852-855_dataset.csv", na.strings = ".")
Variables <- c("caseid", "subtype", "grade", "X5hmc")
DataSelected <- DataFull[, Variables]
Data <- subset(DataSelected, !is.na(X5hmc))
View(Data)
View(Data)
Data <- subset(DataSelected, !is.na(X5hmc), select = -row.names)
?rename
library(plyr)
rename(Data, c("X5hmc" = "fhmc"))
Data <- rename(Data, c("X5hmc" = "fhmc"))
View(Data)
boxplot(fhmc ~ grade)
with(Data, boxplot(fhmc ~ grade))
?levels
?gl
?levels
table(Data&grade)
table(Data$grade)
levels(Data$grade) <- c("Grade 1", "Grade 2", "Grade 3")
table(Data$grade)
Data$grade
revalue(Data$grade, c("1" = Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
revalue(Data$grade, c("1" = "Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
Data$grade <- as.factor(Data$grade)
revalue(Data$grade, c("1" = "Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
with(Data, boxplot(fhmc ~ grade))
table(Data$grade)
Data$grade <- factor(Data$grade)
revalue(Data$grade, c("1" = "Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
with(Data, boxplot(fhmc ~ grade))
table(Data$grade)
str(Data)
revalue(Data$grade, c("1" = "Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
str(Data)
Data$grade <- revalue(Data$grade, c("1" = "Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
str(Data)
with(Data, boxplot(fhmc ~ grade))
DataCase <- ddply(DataSpot, "caseid", summarize, gradeCases = max(grade))
library(plyr)
# Loading the full dataset
DataFull <- read.csv("../tma852-855_dataset.csv", na.strings = ".")
# Selecting the variables for the study
Variables <- c("caseid", "subtype", "grade", "X5hmc")
DataSelected <- DataFull[, Variables]
# Subsetting the data to exclude rows with missing cases
DataSpot <- subset(DataSelected, !is.na(X5hmc))
# Renaming variable of interest
DataSpot <- rename(DataSpot, c("X5hmc" = "fhmc"))
# Converting numeric variable "grade" to factor variable and renaming levels
DataSpot$grade <- factor(DataSpot$grade)
DataSpot$grade <- revalue(DataSpot$grade, c("1" = "Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
View(DataSpot)
DataCase <- ddply(DataSpot, c("caseid", "subtype"), summarize, gradeCase = max(grade), fhmcCase = med(fhmc))
DataCase <- ddply(DataSpot, c("caseid", "subtype"), summarize, gradeCase = max(as.numeric(grade)), fhmcCase = med(fhmc))
?median
DataCase <- ddply(DataSpot, c("caseid", "subtype"), summarize, gradeCase = max(as.numeric(grade)), fhmcCase = median(fhmc))
View(DataCase)
View(DataSpot)
DataSpot <- subset(DataSelected, !is.na(c(X5hmc, grade)))
DataSpot <- rename(DataSpot, c("X5hmc" = "fhmc"))
DataSpot$grade <- factor(DataSpot$grade)
DataSpot$grade <- revalue(DataSpot$grade, c("1" = "Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
DataCase <- ddply(DataSpot, c("caseid", "subtype"), summarize, gradeCase = max(as.numeric(grade)), fhmcCase = median(fhmc))
View(DataCase)
DataSpot <- subset(DataSelected, !is.na(DataSelected))
View(DataSpot)
DataSpot <- subset(DataSelected, !is.na(X5hmc))
DataSpot <- subset(DataSelected, !is.na(grade))
DataSpot <- subset(DataSelected, !is.na(X5hmc))
DataSpot <- subset(DataSpot, !is.na(grade))
DataSpot <- rename(DataSpot, c("X5hmc" = "fhmc"))
DataSpot$grade <- factor(DataSpot$grade)
DataSpot$grade <- revalue(DataSpot$grade, c("1" = "Grade 1", "2" = "Grade 2", "3" = "Grade 3"))
DataCase <- ddply(DataSpot, c("caseid", "subtype"), summarize, gradeCase = max(as.numeric(grade)), fhmcCase = median(fhmc))
View(DataCase)
table(DataCase$gradeCase)
Kwallis
kwallis
??wallis
kruskal.test
?kruskal.test
?nprow
View(DataCase)
table(DataCase$subtype)
DataSpot <- droplevels(DataSpot)
DataCase <- droplevels(DataCase)
table(DataCase$subtype)
kruskal.test(fhmcCase ~ subtype, data = DataCase)
?table
with(DataCase, table(gradeCase, subtype))
??fisher
with(DataCase, fisher.test(gradeCase, subtype))
with(DataCase, plot(gradeCase, subtype))
with(DataCase, plot(subtype, gradeCase))
with(DataCase, plot(subtype, gradeCase, type = "o"))
with(DataCase, plot(subtype, gradeCase, type = "l"))
with(DataCase, dotchart(subtype, gradeCase))
with(DataCase, dotchart(gradeCase, subtype))
with(DataCase, plot(subtype, gradeCase))
with(DataCase, plot(gradeCase, subtype))
with(DataCase, barplot(gradeCase, subtype))
with(DataCase, barplot(subtype, gradeCase))
with(DataCase, barplot(table(gradeCase, subtype)))
with(DataCase, barplot(table(gradeCase, subtype), beside = TRUE))
with(DataCase[subtype == "usual"], boxplot(fhmcCase ~ gradeCase, main = "Usual SCC"))
with(DataCase[subtype == "usual",], boxplot(fhmcCase ~ gradeCase, main = "Usual SCC"))
with(DataCase[DataCase$subtype == "usual",], boxplot(fhmcCase ~ gradeCase, main = "Usual SCC"))
with(DataCase, barplot(table(gradeCase, subtype), beside = TRUE))
?nrow
